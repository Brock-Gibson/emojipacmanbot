'use strict';

var mocha = require('mocha');

mocha.describe("Developer: There is no I in TEAM", () => {
  mocha.it("Tester: We cannot spell BUGS without U", () => {});
  mocha.it("TODO: Write tests here someday", () => {});
});
